// AdMob integration is enabled in production EAS builds only.
// The native module requires a proper EAS build with valid app credentials.

export function showInterstitialIfReady(_isPremium: boolean, _probability = 0.4): void {}

export function initAds(): void {}
